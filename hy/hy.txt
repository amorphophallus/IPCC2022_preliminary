下面简单介绍运行平台的软硬件环境。运行平台两个节点各有2颗32核CPU，节点间通过56Gb高速网互联。

操作系统为 CentOS Linux 7.9，并行环境为 mpiicc 17.0.5 版本。

程序主要分为三个模块。一是递归枚举所有支撑点的组合。二是暴力计算切比雪夫距离之和。三是将当前结果和原有答案序列排序，将当前结果合并到答案中。

初始代码复杂度为 O(n^(k+2))，第一个算例中 O(n^2) 的计算距离函数为计算效率瓶颈，而在第二个算例中 O(n^k) 的枚举为瓶颈。

在下一页的图中简单介绍了程序结构。